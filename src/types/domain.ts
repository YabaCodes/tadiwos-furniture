export type PublicationStatus = "draft" | "published" | "archived";
export type ProductType = "standard" | "configurable" | "made_to_measure";
export type PriceMode =
  | "fixed"
  | "starting_from"
  | "variant"
  | "per_meter"
  | "per_square_meter"
  | "per_piece"
  | "quote_only"
  | "custom";
export type ServiceStatus = "included" | "not_included" | "depends";
export type ProjectType = "residential" | "office" | "commercial" | "institutional";
export type InquiryStatus = "new" | "contacted" | "quoted" | "confirmed" | "closed" | "cancelled";
